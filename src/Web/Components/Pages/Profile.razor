@page "/profile"

<PageHead Title="Profile" Subtitle="Your details, sign-in methods, and regional preferences." />
<ComingSoon Icon="user" Title="Profile — next up"
            ChildText="Display name, default state, and Google / local sign-in management (Identity + Google auth already wired in Program.cs)." />
